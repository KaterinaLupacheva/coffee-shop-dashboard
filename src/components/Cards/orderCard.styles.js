export const orderCardStyles = (theme) => ({
  root: {
    display: "flex",
    flexGrow: 1,
    margin: theme.spacing(1),
    padding: theme.spacing(1),
  },
  orderContainer: {
    display: "flex",
    flexDirection: "column",
  },
});
