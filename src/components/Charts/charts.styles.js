export const chartStyles = (theme) => ({
  container: {
    width: "80vw",
    margin: "0 auto",
  },
  options: {
    display: "flex",
    justifyContent: "flex-start",
    padding: theme.spacing(2),
  },
  select: {
    width: "25ch",
  },
  fieldFontSize: {
    fontSize: 20,
  },
});
